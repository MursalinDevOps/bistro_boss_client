import { Helmet } from 'react-helmet-async';
const Menu = () => {
    return (
        <div className="min-h-screen">
            <Helmet>
                <title>Bistro Boss | Menu</title>
            </Helmet>
            MENU
        </div>
    );
};

export default Menu;